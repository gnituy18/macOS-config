general {
        colors = true
        interval = 5
        color_good = "#8bc34a"
        color_bad = "#f44336"
        color_degraded = "#eceff1"
}

order += "tztime local"

tztime local {
        # format = "%Y-%m-%d %H:%M:%S"
	format = "%Y年 %m月 %d日  %H:%M"
}
